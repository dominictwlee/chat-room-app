import React, { Fragment } from 'react';

import styles from './MessageInput.module.css';

const MessageInput = () => <input className={styles.textInput} type="text" />;

export default MessageInput;
